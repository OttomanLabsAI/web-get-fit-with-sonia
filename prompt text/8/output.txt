v1.7 is released. On a phone the home page is now less than half as long: at 390px wide it went from 16,867px, about twenty screens, to 7,750px, about nine, with nothing taken out. At 320px it went from 17,960px to 8,736px.

The biggest saving is Classes. Phones now get the same class buttons as desktop, two to a row, and only the chosen class shows instead of all six stacked. Each timetable session fits on two lines (time, class and map link, then the venue), so a screen holds nearly twice as many. The prices sit side by side, the facts strip is two by two, Sonia's portrait sits beside her welcome, and the gallery photos go two to a row in their own shapes and still open large when tapped. The two short testimonials share a row, and the nine venues are one row you swipe sideways. On the smallest phones the header now fits on a single row. Phones also download about a quarter less, because the portrait no longer loads at full-screen size.

The Improvements page is shorter on a phone too. Each old screenshot sits beside its new one, the new ones are retaken to show the current layout, and its page-size figure is corrected to about 360 KB. Desktop is unchanged apart from one fact that now wraps more neatly.

**Verified.** Page heights were measured at every phone width from 320px to 414px. On a phone, tapping each class button shows that class, links to a class open it, photos open large at their true shape, and the venue row scrolls without widening the page. The class buttons also work with JavaScript off. Desktop matches v1.6 screen for screen apart from that one fact. The layout sweep is clean on all five pages at thirteen widths from 320px to 1920px, the HTML is valid, axe finds zero violations, the timetable still prints on one sheet, and the wrangler dry run passes.