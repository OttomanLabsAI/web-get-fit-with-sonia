v1.3 is released: the demo bar is on every page, a working copy of the old site sits behind its Current site tab, the gallery is a centred composition, and the cards now say Testimonial.

**The demo bar.** Built with the site-pitch skill's bar, pinned above everything on every page: the Demo chip, then New site, Current site and Improvements, with the current tab underlined. The old header pill is gone. On the narrowest phones the bar scrolls sideways rather than overflowing.

**Current site.** A faithful copy of the old WordPress site made from the seven pages you saved, following the skill's capture rules: scripts, trackers, search hints and every reference back to the live host removed, assets served locally, forms unable to submit anywhere, noindex, and a dated snapshot strip at the top saying what it is with a link to the live site. It reproduces the old site's phone-width overflow, which is the point of the comparison. It has its own security policy so the old theme's inline styles work while scripts stay off. Nothing on it makes a request to the live host or to Google Fonts. Links to things the saved pages did not include, such as Register, Login and the full-size gallery images, still lead to the live site.

**Gallery.** A centred flow: rows share a height, each photo keeps its own proportions and width, and rows centre so the sides need not line up. On desktop the two group shots run large with the four smaller pictures in a row beneath; on phones each photo takes the full width. Portrait originals will slot in tall beside the landscape ones when they arrive. Nothing is cropped or stretched on the page today.

**Verified.** Valid HTML on the new site's five pages, the layout sweep clean at thirteen widths from 320px to 1920px, zero axe violations, every reference resolving across all twelve pages, the wrangler dry run passing, and under wrangler dev one security policy per page with the copy getting its own. The copy's own sweep shows zero leaks and the expected overflow at 320px to 414px, which is the old site's, not ours.

The screenshot pasted into the "change review to testimonial" message arrived as pixels only, so it could not be saved into the prompt archive.

**Still needed from Sonia:** the full-size originals of the gallery photos, listed in the brief, and the earlier questions on the Improvements page.